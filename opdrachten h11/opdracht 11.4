int[] mijnArray = new int[10];

void setup(){
  for(int i = 0; i < mijnArray.length; i++){
    mijnArray[i] = 12+i*12;
  }
  
  for(int i = 0; i < mijnArray.length; i++){
    println(mijnArray[i]);
  }
}
